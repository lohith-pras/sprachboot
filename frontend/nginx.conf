server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # Next static export emits per-route .html files; fall back through them
    # before the SPA index so clean URLs (/speak, /analytics) resolve.
    location / {
        try_files $uri $uri.html $uri/ /index.html;
    }
}
